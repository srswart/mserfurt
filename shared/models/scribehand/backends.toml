# TD-018 generation backends for `scribesim --neural-backend <name>`.
#
# `stub-pil` and `stub-evo` are built in (CPU dev). Entries here define
# CommandBackend runners for the fine-tuned models on the GPU workstation.
# Uncomment and adjust paths after fine-tuning (docs/scribehand-mac-runbook.md).

schema = 1

# [backends.onedm]
# argv = ["python", "/ABS/PATH/mserfurt/scripts/scribehand/onedm_runner.py"]
# workdir = "/ABS/PATH/One-DM"            # upstream repo clone (imports resolve here)
# checkpoint = "/ABS/PATH/One-DM/ckpt/onedm_bastarda_v1.pt"
# style_dir = "/ABS/PATH/mserfurt/shared/models/scribehand/style_anchor_v1"

# [backends.diffusionpen]
# argv = ["python", "/ABS/PATH/mserfurt/scripts/scribehand/diffusionpen_runner.py"]
# workdir = "/ABS/PATH/DiffusionPen"
# checkpoint = "/ABS/PATH/DiffusionPen/ckpt/diffusionpen_bastarda_v1"
# style_dir = "/ABS/PATH/mserfurt/shared/models/scribehand/style_anchor_v1"
