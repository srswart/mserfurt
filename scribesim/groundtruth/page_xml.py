"""Generate PAGE XML 2019 ground truth — stub pending ADV-SS-GROUNDTRUTH-001."""

from __future__ import annotations

from pathlib import Path


def generate(layout: dict, output_path: Path) -> Path:
    """Emit PAGE XML with glyph-level coordinates and TextEquiv (TD-001-C).

    Raises:
        NotImplementedError: Until ADV-SS-GROUNDTRUTH-001 is implemented.
    """
    raise NotImplementedError("Ground truth not yet implemented (ADV-SS-GROUNDTRUTH-001)")
